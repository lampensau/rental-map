import type { PageLoad } from './$types';

// This is just a placeholder for the route
// The actual admin functionality is implemented as a modal in RentalApp.svelte
export const load: PageLoad = async () => {
  return {};
}; 